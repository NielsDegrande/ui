packages:
  - "."

# Dependencies allowed to run build scripts on install.
onlyBuiltDependencies:
  - msw
  - unrs-resolver

# Security overrides for transitive dependencies awaiting upstream bumps.
overrides:
  "js-yaml@<4.3.1": ">=4.3.1"
