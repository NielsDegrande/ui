import { Color } from "src/utils/colors";

export default function palette(): Record<string, unknown> {
  return {
    primary: {
      main: Color.PRIMARY,
    },
    // secondary: {
    //   main: Color.SECONDARY,
    // },
  };
}
