/* eslint-disable @typescript-eslint/no-duplicate-enum-values */

/**
 * Capture all colors used in the UI to ensure consistency.
 */
export enum Color {
  // Color for global background.
  BACKGROUND = "white",
  // Primary color for elements, e.g., buttons.
  PRIMARY = "#68b0ab",
  // Soft primary color, e.g., when hovering.
  PRIMARY_SOFT = "#c4ebe8",
  // Secondary color for elements, e.g., buttons.
  // SECONDARY = "#e396df",
  // Soft secondary color, e.g., when hovering.,
  // SECONDARY_SOFT = "#feebff",
  // Text color for primary or secondary backgrounds.
  TEXT = "white",
  // Text color for global background.
  TEXT_BACKGROUND = "black",
  // Text color for disabled elements.
  // TEXT_DISABLED = "#ede7e3",
  // Text color for soft backgrounds.
  TEXT_SOFT = "black",
}
